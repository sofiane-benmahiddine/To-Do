spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/todo
    username: postgres
    password: spintechs
  jpa:
    properties:
      dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
  application:
    name: todo-app
logging:
  level:
    ${spring.application.name}: debug
  file:
    path: './logs'
    name: ${logging.file.path}/todo.log
app:
  jwt:
    expiration-period: 14
    key: Yp3s6v9y$B&E)H@McQfTjWnZq4t7w!z%